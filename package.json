{
    "name": "AsyncJS",
    "preferGlobal": true,
    "version": "0.0.1",
    "author": "Olaf Erlandsen",
    "description": "Trick for better speed and compatibility",
    "license": "MIT",
    "engines": {
        "node": ">=0.10"
    },
    "scripts": {
        "test": "node ./src/AsyncJS.js",
        "coverage": "`npm bin`/istanbul cover ./src/AsyncJS.js",
        "coveralls": "npm run-script coverage && node bin/coveralls.js < coverage/lcov.info"
    },
    "dependencies": {
        "express": "~3.4.7",
        "json-lint": "~0.1.0"
    }
}
